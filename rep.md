# 🧮 科学计算器 (Java Desktop Calculator) 开发需求

## 1. 项目概述
请使用 Java Swing 开发一个功能强大的桌面计算器。要求不仅能处理点击输入，还能解析复杂的数学表达式字符串。

## 2. 核心功能
* **基础运算**：加 (+)、减 (-)、乘 (*)、除 (/)。
* **高级运算**：支持括号 `()`、百分号 `%`、正负号切换。
* **表达式解析**：用户可以输入如 `(10 + 5) * 2 / 5` 这样的长表达式，按下等号后给出正确结果。
* **错误处理**：
  * 处理“除以零”的情况并显示友好提示（如 "Error"）。
  * 捕获并处理非法数学表达式（如括号不匹配）。

## 3. 界面 (UI) 要求
* **布局**：使用 `GridBagLayout` 实现按钮对齐，上方为多行文本框（显示计算过程和结果）。
* **配色**：深色模式 (Dark Mode) 或 macOS 风格配色，按钮要有悬停高亮效果。
* **响应式**：窗口拉伸时，按钮大小能自动调整。

## 4. 技术深度建议 (核心考点)
* **算法**：请使用 **调度场算法 (Shunting-yard Algorithm)** 或 **逆波兰表达式 (RPN)** 来处理运算符优先级，不要直接调用邪门脚本引擎。
* **设计模式**：尝试使用 **命令模式 (Command Pattern)** 来处理按钮点击，或使用 **工厂模式** 创建按钮。